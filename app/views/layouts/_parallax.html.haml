.parallax-container
  .parallax
    -# = image_tag 'main_header.jpg', alt: 'Main header', width: '100%'
    %img.responsive-img{:src => "images/main_header.jpg"}

  -#.valign-wrapper{style: 'height: 500px' }
    %h4.valign.white-text.center
      %em= t('marketing.tagline')

.section.white
  .row.container
    %h2.header Parallax
    %p.grey-text.text-darken-3.lighten-3 Parallax is an effect where the background content or image in this case, is moved at a different speed than the foreground content while scrolling.

    %h2.header Parallax
    %p.grey-text.text-darken-3.lighten-3 Parallax is an effect where the background content or image in this case, is moved at a different speed than the foreground content while scrolling.

    %h2.header Parallax
    %p.grey-text.text-darken-3.lighten-3 Parallax is an effect where the background content or image in this case, is moved at a different speed than the foreground content while scrolling.

    %h2.header Parallax
    %p.grey-text.text-darken-3.lighten-3 Parallax is an effect where the background content or image in this case, is moved at a different speed than the foreground content while scrolling.
